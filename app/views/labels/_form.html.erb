<div class="form-group">
    <%= f.label :labels %>
 <!-- use a text field so that a user can submit multiple labels -->
    <%= f.text_field :labels,
                     class: 'form-control',
                     placeholder: "Enter labels (separated by commas)",
                     value: labels.map(&:name).join(', ') %>
  </div>
