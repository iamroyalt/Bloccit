<% unless labels.empty?  %>
<!-- if the labels array is not empty, we render the labels using a wishfully coded helper method named labels_to_buttons -->
   <%= labels_to_buttons(labels) %>
 <% end %>
